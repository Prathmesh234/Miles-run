# Charts pending runtime evidence

Job 197 was queued at this snapshot. No runtime results or performance curves are fabricated. Run `../scripts/collect_evidence.py`, then `../scripts/plot_run.py` after data exists. The latter renders GPU telemetry and measured async stage durations to PNG and SVG and records source hashes.
